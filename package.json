{
  "name": "basket-tournament",
  "version": "1.0.0",
  "author": "Julien BOUYER",
  "private": true,
  "devDependencies": {
    "browser-sync": "^2.26.7",
    "debug": "~2.6.9",
    "eslint": "5.12.1",
    "npm-run-all": "4.1.5",
    "rimraf": "^2.6.3"
  },
  "dependencies": {
    "bootstrap": "^4.3.1",
    "cookie-parser": "~1.4.3",
    "cors": "^2.8.4",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "material-design-icons": "^3.0.1",
    "morgan": "~1.9.0",
    "mysql": "^2.17.1",
    "pug": "^2.0.4",
    "vue": "^2.6.10"
  },
  "scripts": {
    "db:build": "cd docker && docker-compose build",
    "db:up": "cd docker && docker-compose up -d",
    "db:down": "cd docker && docker-compose down",
    "clean": "npm install rimraf && npm run clean:modules",
    "clean:modules": "rimraf ./node_modules",
    "start": "run-p start:*",
    "start:node": "node ./bin/www",
    "start:server": "browser-sync start --no-notify --proxy 'http://localhost:3000/' -f './public/**'"
  }
}
